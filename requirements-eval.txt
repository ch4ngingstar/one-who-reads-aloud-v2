# Evaluation harness dependencies (scripts/eval_*.py).
# Kept OUT of the core requirements.txt so the runtime pipeline stays lean —
# install only when running the eval/measurement harness:
#     pip install -r requirements-eval.txt
#
# All CPU-capable. resemblyzer pulls torch, which the pipeline already has.

faster-whisper>=1.0.0      # ASR for TTS accuracy (WER/CER). CPU int8 is fine.
jiwer>=3.0.0               # WER / CER computation
librosa>=0.10.0            # prosody features (F0, RMS energy, duration)
soundfile>=0.12.0          # WAV I/O for feature extraction
resemblyzer>=0.1.1         # speaker-embedding cosine similarity (voice consistency)
numpy>=1.24.0              # (already transitively present) array math
